[distutils]
index-servers=
    {%- if PYPI != "" and PYPI is defined %}pypi{% endif %}
    test
    
{%- if PYPI != "" and PYPI is defined %}
[pypi]
{{ PYPI }}
{%- endif %}

[test]
repository: https://test.pypi.org/legacy/
