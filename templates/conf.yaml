---
# This is the configuration for the Release Bot
gitchangelog    : {{ environ('CHANGELOG') }}
github_token    : {{ environ('GITHUB_TOKEN') }}
github_username : {{ environ('GITHUB_ACTOR') }}
repository_name : {{ environ('GITHUB_REPOSITORY').split('/')[1] }}
repository_owner: {{ environ('GITHUB_REPOSITORY').split('/')[0] }}
